//const boxList = document.getElementsByClassName('box')


// =================================================================================================================
// beg: responsive
// =================================================================================================================
if ( ( window.innerWidth > 800 ) && ( window.innerHeight > 600)) {
    // desktop
} else {
    // mob
    // console.log("makng 49% -> 100%")
    // for(var i=0; i<boxList.length; i++) {
    //     boxList[i].style.width = "100%"
    // }
}